# Deployment trigger

Repository ownership moved to `sinthpad-ship` and the Vercel project was reconnected on 2026-09-01.

This commit intentionally triggers a fresh production deployment from `main` so the current HeadphonesBase application replaces the pre-transfer production build.
